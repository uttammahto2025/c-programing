#include <stdio.h>
int main()
{
    int a, b, Sum;
    printf("Enter two integers: ");
    scanf("%d %d", &a, &b);
    Sum = a + b;
    printf("Adition of the given integers : %d\n", Sum);
    return 0;
    
}